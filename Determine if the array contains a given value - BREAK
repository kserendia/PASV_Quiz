Write a function called isElementIncluded that takes a numeric array arr and a number x as arguments and returns true if the array has x, and false otherwise.

You must use the break statement in your solution.

Examples:

function isElementIncluded ([10, 0, 4, 5, 9, 30], 0) must return true;
isElementIncluded ([2, 1, -3, 1, 4, 3], 1) must return true;
isElementIncluded ([2, 1, -3, -4, 3], 0) must return false.


Solution:

function isElementIncluded(arr, x){
  for (let i = 0; i < arr.length; i++){
    if (arr[i] === x){
      return true;
      break;
    }
  }
  return false;
}
